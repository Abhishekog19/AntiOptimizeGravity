# Antigravity Quota Tracker — Python Dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Versions are pinned to what was tested and verified working.
# Update with care — pystray and pywebview in particular have breaking changes
# between minor versions.

# ── CDP watcher ───────────────────────────────────────────────────────────────
requests>=2.28.0,<3          # HTTP calls to dashboard; CDP HTTP list-targets
psutil>=5.9.0,<8             # process detection (Antigravity launch/close)
websocket-client>=1.3.0,<2   # CDP WebSocket transport (Network domain listener)

# ── Dashboard server ──────────────────────────────────────────────────────────
flask>=3.0.0,<4              # embedded HTTP dashboard server

# ── System tray icon ──────────────────────────────────────────────────────────
pystray>=0.19.5,<1           # cross-platform system tray icon
pillow>=10.0.0,<13           # icon rendering (tray icon colour + image ops)

# ── Native dashboard window ───────────────────────────────────────────────────
pywebview>=4.4.0,<7          # native window backed by WebView2 (Win) / WKWebView (Mac)

# ── Windows toast notifications ───────────────────────────────────────────────
win10toast>=0.9,<1           # Windows 10/11 toast notifications (Windows-only)
# plyer>=2.1.0               # cross-platform alternative — uncomment for Mac/Linux
