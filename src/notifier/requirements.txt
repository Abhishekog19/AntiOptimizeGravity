# Antigravity Quota Tracker — Notifier Dependencies
#
# The full dependency list has moved to the root requirements.txt.
# Install from the project root with:
#
#   pip install -r requirements.txt
#
# This file is kept for tools that auto-discover requirements.txt in subdirectories.
# It simply redirects to the root file.
#
# (Note: pip does not support -r relative-file redirects between directories,
#  so just run the root requirements.txt directly from the project root.)
